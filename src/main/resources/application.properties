# Fooodie Application Configuration

# Application Name
app.name=Fooodie Food Delivery Service
app.version=1.0.0

# Database Configuration (for future use)
# db.driver=org.sqlite.JDBC
# db.url=jdbc:sqlite:fooodie.db
# db.user=
# db.password=

# Logging
log.level=INFO
log.file=logs/app.log

# Server Configuration (for future REST API)
# server.port=8080
# server.host=localhost

